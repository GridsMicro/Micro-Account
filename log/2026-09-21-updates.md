# บันทึกการทำงาน - 2026-09-21 (รอบเตรียมงานภาษีรายเดือน)

## วัตถุประสงค์
- พี่ฆังมอบหมายให้เรียนรู้โครงการ Micro-Account และบันทึกการทำงาน
- เราจะเข้ามาทำงานในโปรเจกต์นี้ **เดือนละครั้ง ประมาณวันที่ 20** เฉพาะงานด้านภาษี

## ภาพรวมโครงการ (เรียนรู้แล้ว)
- **Micro-Account** = ระบบบัญชี/ภาษีอัตโนมัติ Next.js 16 + Neon PostgreSQL + RBAC
- บริษัทเป้าหมาย: MICROTRONIC (THAILAND) CO., LTD. / TAX ID `0105561182888`
- หน้า E-Filing Dashboard: `app/tax-reports/page.tsx` รองรับ ภ.พ.30, ภ.พ.36, ภ.ง.ด.3, ภ.ง.ด.53, ภ.ง.ด.51
- ตารางผังบัญชีที่เกี่ยวข้อง (docs/BUSINESS_CONTEXT.md): 1141 ภาษีซื้อ, 2121 ภาษีขาย, 2130 ภาษีหัก ณ ที่จ่ายค้างจ่าย
- กำหนดยื่น: ภ.พ.30 / ภ.ง.ด.3 / ภ.ง.ด.53 / ภ.พ.36 ภายในวันที่ 15 ของเดือนถัดไป → เราเข้ามาทำวันที่ ~20 เช่น ตรวจ/แก้/เตรียมไฟล์ของเดือนก่อนหน้า

## สถานะฟีเจอร์ภาษีที่ตรวจพบ (ข้อเท็จจริงจากโค้ด — สำคัญ)
| ฟีเจอร์ | สถานะ | หลักฐาน |
|--------|--------|---------|
| PP.30 Draft (ภาษีขาย-ซื้อ รายเดือน) | ✅ query DB จริง | `app/actions/tax-reports.ts:26` |
| PP.30 Export เป็นไฟล์ .txt | ⚠️ **ยังเป็น DUMMY** | `app/actions/tax-reports.ts:51-53` คืน `Buffer.from("DUMMY")` |
| PND 53 Export เป็นไฟล์ .txt | ⚠️ **ยังเป็น DUMMY** | `app/actions/tax-reports.ts:55-57` คืน `Buffer.from("DUMMY")` |
| PND 3 / PND 53 Draft (WHT) | ⚠️ stub คืนค่าว่าง | `app/actions/tax-reports.ts:43-45` `items: []; totalWHT: 0` |
| PP.36 Draft (ต่างประเทศ) | ⚠️ stub คืนค่าว่าง | `app/actions/tax-reports.ts:47-49` |
| ส่งข้อมูลเข้ากรมสรรพากร (RD API) | ⚠️ dummy ทั้งหมด | `app/actions/tax-reports.ts:59-61`; ปุ่มใน UI ส่ง `sampleIds ["1","2","3"]` (`app/tax-reports/TaxExportButton.tsx:61`) |
| RD API client (lib/rd-api.ts) | ✅ โครงสร้างครบ ต้อง initialize config | `initializeRDClient()` |
| Tax Automator (lib/taxAutomator.ts) | ✅ มี WHT engine, VAT validator, ปฏิทินภาษี | ใช้ได้ |

### ข้อควรระวังที่พบ
- หน้า Dashboard แสดง "WHT" จาก `getTaxSummary` เป็น 0 เสมอ (`wht: 0`) ยังไม่ได้ query จริง
- PP.30 Draft คำนวณ VAT ซื้อจาก `amount * 0.07` (`tax-reports.ts:31`) ไม่ใช่ `vat_amount` จริงใน voucher → ตัวเลขอาจคลาดเคลื่อน ต้องตรวจสอบกับสมุดรายวัน/ใบกำกับภาษีซื้อจริงก่อนยื่น
- ถ้าเรากด "ส่งออกไฟล์" หรือ "ส่ง RD API" บนหน้าจริงตอนนี้ จะได้ไฟล์ DUMMY / ผล dummy → **ห้ามใช้ยื่นจริง** ต้อง implement หรือใช้วิธี manual

## แผนงานรอบถัดไป (วันที่ 20 ของเดือน)
1. ตรวจสอบยอดภาษีขาย-ซื้อ ประจำเดือนในหน้า `/tax-reports` เทียบกับสมุดรายวัน (`/journals`)
2. ตรวจ WHT จาก payment vouchers เทียบกับ ภ.ง.ด.3/53
3. ตรวจ VAT ต่างประเทศ (ภ.พ.36) จาก expense ที่ flag overseas
4. เตรียมข้อมูลสำหรับยื่น e-Filing (ภายในวันที่ 15 เดือนถัดไป) — ถ้า export ยังเป็น DUMMY ให้ดึงตัวเลขจากหน้า dashboard + เอกสารจริง
5. งานค้าง/ปรับปรุง (ให้ consult พี่ฆังก่อน): ทำให้ export .txt เป็นข้อมูลจริง, ทำให้ PND/PP36 draft query DB จริง ปิดช่องว่าง WHT=0 และ VAT ซื้อ = amount*0.07

## หมายเหตุ
- Git สถานะ: branch `main` ขึ้นกับ origin/main; มีไฟล์ที่ยังไม่ได้ commit: `next-env.d.ts`, `pnpm-lock.yaml`, `.pnpm-allow-scripts.cjs`, `.pnpmfile.cjs` (ไม่ใช่ของรอบนี้ ไม่แตะ)
- governance: `npm run check:knowledge` / `check:consistency` / `npm test` ใช้ตรวจก่อน deploy

## ปัญหาการเปิดเซสชันเฉพาะโปรเจกต์ (แก้แล้ว 2026-09-21)
- **อาการ:** ไม่สามารถเปิด session เฉพาะ Micro-Account ได้; เปิดมาแล้วร่วงไปที่ "พื้นที่ใหญ่นอกสุด" (project `global` `/`)
- **สาเหตุที่พิสูจน์แล้ว:** โปรเจกต์ถูกย้ายโฟลเดอร์จาก `~/workspace/services/next/Micro-Account` → `01-PRODUCTS-BUSINESS/Micro-Account` แต่ใน `~/.local/share/opencode/opencode.db` ตาราง `project` id `826f81ef...` ยังมี `worktree` = path เดิมที่ไม่มีอยู่จริง และ `session` เก่า 4 อันผูก directory เดิม
- **วิธีเปิดใช้:** `cd "/home/neon13/workspace/services/next/01-PRODUCTS-BUSINESS/Micro-Account" && opencode`
- **แก้ใน DB แล้ว (2026-09-21):** อัปเดต `project.worktree` / `project_directory` / `session.directory` ให้ชี้ path จริงของ Micro-Account + 5 โปรเจกต์ที่ถูกย้าย (Micro-Menu-Online, TarotCardsV.1, Nung-Sue-Suat-Mon, Micro-Cards-Maker, microtronic.dev) → scan หลังแก้: worktree ทุกโปรเจกต์อยู่จริง 100%
  - Backup: `~/opencode-db-backup-20260921-010906.db` (ก่อนแก้ Micro-Account) และ `~/opencode-db-backup-20260921-010944.db` (ก่อนแก้ 5 โปรเจกต์)
  - หมายเหตุ: ต้อง restart opencode ครั้งถัดไปเพื่อให้ UI เห็นโปรเจกต์กลับมาในรายการ

### เก็บกวาด session ผี (เพิ่มเติม 2026-09-21 ~01:19)
- **ลบ session ผี 4 อัน** ที่ `project_id=global` แต่ `directory`/`path` ชี้ `~/workspace/services/next/Micro-Account` (path เก่า, ไม่มีจริง) พร้อม message/part/event ที่ผูก (ลบไป 4 parts, 8 messages, 112 events) — session เหล่านี้คือตัวที่ UI หยิบขึ้นโชว์เป็น "Micro-Account" ทั้งที่อยู่ global
- **แก้ position ของ session จริง 4 อัน** ที่ผูกกับโปรเจกต์ที่ถูกย้าย แต่ยังชี้ global/path เก่า:
  - `ses_fa3684a7c...` (ตรวจสอบโปรเจกต์, 39 msg) → `02-FORTUNE-WEB/TarotCardsV.1` (proj `86985876...`)
  - `ses_f993050acf...` (ออกแบบแอป QR เมนู, 391 msg) `ses_f992de646f...` (6 msg) `ses_f95c4d7a3f...` (2 msg) → `01-PRODUCTS-BUSINESS/Micro-Menu-Online` (proj `cd1c24ec...`)
- **แก้ path โปรเจกต์อื่นที่ย้ายด้วย** (2 sessions ของ podcast-gen): `~/workspace/apps/podcast-gen` → `~/workspace/services/python/podcast-gen`
- **ยืนยันผล:** สแกนทั้งตาราง = 122 sessions ชี้ path ที่มีอยู่จริงทั้งหมด 0 อันค้าง path เก่า
  - Backup: `~/opencode-db-backup-20260921-011927.db`
  - ยังต้อง **restart opencode** ครั้งต่อไปเพื่อให้ UI โหลดข้อมูลใหม่

---
*บันทึกโดย: ฌอน (ผู้แทน opencode ของพี่ฆัง)*